# Core
pandas>=2.0.0
numpy>=1.24.0

# Data fetching (Taiwan financial data)
requests>=2.31.0
# Optional: FinMind for Taiwan financial data
# finmind>=1.7.0

# ML / stats
scipy>=1.11.0
scikit-learn>=1.3.0   # for Isolation Forest comparison

# LLM (optional — only if running llm_summary.py)
anthropic>=0.34.0

# Notebook (optional)
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
